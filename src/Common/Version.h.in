#pragma once

#define MAJOR_VERSION ${CONFIG_VERSION_MAJOR}
#define MINOR_VERSION ${CONFIG_VERSION_MINOR}
#define REVISION_VERSION ${CONFIG_VERSION_BUG}
// #define CASPAR_TAG "${CONFIG_VERSION_TAG}"
#define BUILD_VERSION "${GIT_VERSION}"
#define BUILD_TIMESTAMP "${GIT_TIMESTAMP}"

#define RC_VERSION "${CONFIG_VERSION_MAJOR}.${CONFIG_VERSION_MINOR}.${CONFIG_VERSION_BUG} ${GIT_VERSION}"

#define DATABASE_VERSION "216"
